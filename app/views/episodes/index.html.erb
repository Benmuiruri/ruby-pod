<h1><%= @podcast.name %></h1>

<% @episodes.each do |episode| %>
  <div>
    <h2><%= episode.title %></h2>
    <p><%= truncate(episode.description, length: 100) %></p>
    <p><%= episode.release_date %></p>
    <p><%= link_to "Listen on Spotify", episode.spotify_url %></p>
  </div>
<% end %>
